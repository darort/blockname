{
    "redNotes": [
        { "pattern": ".*error.*", "note": "Error detected" },
        { "pattern": "failed", "note": "Failed" }
    ],
    "greenNotes": [
        { "pattern": "success", "note": "Success" }
    ],
    "paidSign": "ok"  // Add this field to specify the sign
}
